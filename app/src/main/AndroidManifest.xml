<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.first.kritikm.split">

    <application
        android:name="com.first.kritikm.split.GlobalData"
        android:allowBackup="true"
        android:hardwareAccelerated="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.first.kritikm.split.MainActivity" />
        <activity
            android:name="com.first.kritikm.split.HomeActivity"
            android:label="@string/title_activity_home" />
        <activity android:name="com.first.kritikm.split.IntroActivity">
        </activity>
        <activity android:name="com.first.kritikm.split.EventDetailsActivity" />
        <activity android:name=".BasicSplit" />
        <activity android:name="com.first.kritikm.split.SplitDetailsActivity" />
        <activity android:name="com.first.kritikm.split.SplashScreen"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
    </application>

</manifest>